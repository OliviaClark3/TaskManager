import { Typography } from '@mui/material';
import React from 'react';

const RightSideBar = () => {
    return (
        <Typography>righter</Typography>
    )
}

export default RightSideBar;